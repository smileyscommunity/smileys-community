# dependencies
node_modules/
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
.env
.env.*
!.env.example

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/lib/generated/prisma

# uploaded files
# Uploads live outside public/ so Next can't serve them statically around
# the gated /api/files route — see lib/uploadRoot and SECURITY.md invariant 15.
# public/uploads is deliberately gone, not just ignored: an empty dir there
# invites the next contributor to write into it again.
/uploads/
.env.local

# Claude Code project-local state (settings, agent-installed skills)
.claude/

# Local DB dumps — full member PII, never in git (server keeps its own in /root/db-backups)
backups/
